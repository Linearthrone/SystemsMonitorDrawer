<Window
    x:Class="SystemMonitor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SystemMonitor"
    Title="Overlay"
    Width="450"
    Height="150"
    AllowsTransparency="True"
    Background="Transparent"
    MouseDown="Window_MouseDown"
    ShowInTaskbar="False"
    Topmost="True"
    WindowStyle="None">

    <!--  Semi-transparent container with rounded corners  -->
    <Border
        Background="#CC111111"
        BorderBrush="#333"
        BorderThickness="1"
        CornerRadius="20">
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <!--  The 3 Gauges  -->
            <local:GaugeControl x:Name="CpuGauge" Margin="5" />
            <local:GaugeControl x:Name="GpuGauge" Margin="5" />
            <local:GaugeControl x:Name="RamGauge" Margin="5" />
        </StackPanel>
    </Border>
</Window>
