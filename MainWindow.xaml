<Window
    x:Class="SystemMonitor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SystemMonitor"
    Title="Overlay"
    Width="500"
    Height="175"
    AllowsTransparency="True"
    Background="Transparent"
    MouseDown="Window_MouseDown"
    ShowInTaskbar="False"
    Topmost="True"
    WindowStyle="None">

    <!--  Semi-transparent container with rounded corners  -->
    <Border
        Background="#CC111111"
        BorderBrush="#333"
        BorderThickness="1"
        CornerRadius="20">
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Vertical">
            <!--  Temperature Bar and CPU/GPU Gauges  -->
            <StackPanel
                HorizontalAlignment="Center"
                Orientation="Horizontal"
                Margin="0,10,0,10">
                <local:TemperatureBarControl x:Name="TempBar" Margin="5" />
                <Grid Margin="5">
                    <local:GaugeControl x:Name="CpuGauge" />
                    <local:FanGaugeControl x:Name="CpuFanGauge" VerticalAlignment="Top" HorizontalAlignment="Center" />
                </Grid>
                <local:GaugeControl x:Name="GpuGauge" Margin="5" />
            </StackPanel>
            <!--  RAM Bar  -->
            <local:RamBarControl x:Name="RamBar" Margin="5" />
        </StackPanel>
    </Border>
</Window>
